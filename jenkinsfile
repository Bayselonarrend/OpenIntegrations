pipeline {
    stage('Checkout') {
        steps {
                checkout scm
        }
    }

    stage('Build') {
        steps {
                bat 'oscript ./.github/workflows/os/main.os'
        }
    }
}