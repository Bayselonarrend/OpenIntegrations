#совместимость 9.0

#требуется ./internal/Executor.sbsl

@Глобально
метод Инициализировать(Конфигурация: Строка?|Executor.НастройкиИнтеграции): OPI_YandexDisk
    
    пер Исполнитель = Executor.ИсполнительОПИ.Инициализировать(Конфигурация)
    возврат новый OPI_YandexDisk(Исполнитель)
    
;

@Локально
структура OPI_YandexDisk
    
    обз знч Исполнитель: Executor.ИсполнительОПИ
    
    знч КомандаCLI: Строка = "yadisk"
    
    @Глобально
    метод GetConfirmationCode(ClientId: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-i", ClientId)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetConfirmationCode", Опции)
        
    ;

    @Глобально
    метод ConvertCodeToToken(ClientId: Объект?, ClientSecret: Объект?, DeviceCode: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-i", ClientId)
        Опции.Вставить("-s", ClientSecret)
        Опции.Вставить("-d", DeviceCode)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ConvertCodeToToken", Опции)
        
    ;

    @Глобально
    метод RefreshToken(ClientId: Объект?, ClientSecret: Объект?, RefreshToken: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-i", ClientId)
        Опции.Вставить("-s", ClientSecret)
        Опции.Вставить("-r", RefreshToken)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "RefreshToken", Опции)
        
    ;

    @Глобально
    метод GetDiskInformation(Token: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetDiskInformation", Опции)
        
    ;

    @Глобально
    метод CreateFolder(Token: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "CreateFolder", Опции)
        
    ;

    @Глобально
    метод GetObject(Token: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetObject", Опции)
        
    ;

    @Глобально
    метод DeleteObject(Token: Объект?, Path: Объект?, ToCart: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-c", ToCart)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "DeleteObject", Опции)
        
    ;

    @Глобально
    метод CreateObjectCopy(Token: Объект?, Original: Объект?, Path: Объект?, Overwrite: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-f", Original)
        Опции.Вставить("-o", Path)
        Опции.Вставить("-r", Overwrite)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "CreateObjectCopy", Опции)
        
    ;

    @Глобально
    метод GetDownloadLink(Token: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetDownloadLink", Опции)
        
    ;

    @Глобально
    метод GetFileUploadLink(Token: Объект?, Path: Объект?, Overwrite: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-r", Overwrite)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetFileUploadLink", Опции)
        
    ;

    @Глобально
    метод DownloadFile(Token: Объект?, Path: Объект?, SavePath: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        Опции.Вставить("", SavePath)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "DownloadFile", Опции)
        
    ;

    @Глобально
    метод GetFilesList(Token: Объект?, Count: Объект? = Неопределено, OffsetFromStart: Объект? = Неопределено, FilterByType: Объект? = Неопределено, SortByDate: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-a", Count)
        Опции.Вставить("-o", OffsetFromStart)
        Опции.Вставить("-p", FilterByType)
        Опции.Вставить("-d", SortByDate)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetFilesList", Опции)
        
    ;

    @Глобально
    метод MoveObject(Token: Объект?, Original: Объект?, Path: Объект?, Overwrite: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-f", Original)
        Опции.Вставить("-o", Path)
        Опции.Вставить("-r", Overwrite)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "MoveObject", Опции)
        
    ;

    @Глобально
    метод UploadFile(Token: Объект?, Path: Объект?, File: Объект?, Overwrite: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-f", File)
        Опции.Вставить("-r", Overwrite)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "UploadFile", Опции)
        
    ;

    @Глобально
    метод UploadFileInParts(Token: Объект?, Path: Объект?, File: Объект?, ChunkSize: Объект? = Неопределено, Overwrite: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-f", File)
        Опции.Вставить("-s", ChunkSize)
        Опции.Вставить("-r", Overwrite)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "UploadFileInParts", Опции)
        
    ;

    @Глобально
    метод UploadFileByURL(Token: Объект?, Path: Объект?, Address: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-u", Address)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "UploadFileByURL", Опции)
        
    ;

    @Глобально
    метод PublishObject(Token: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "PublishObject", Опции)
        
    ;

    @Глобально
    метод CancelObjectPublication(Token: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "CancelObjectPublication", Опции)
        
    ;

    @Глобально
    метод GetPublishedObjectsList(Token: Объект?, Count: Объект? = Неопределено, OffsetFromStart: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-a", Count)
        Опции.Вставить("-o", OffsetFromStart)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetPublishedObjectsList", Опции)
        
    ;

    @Глобально
    метод GetPublicObject(Token: Объект?, URL: Объект?, Count: Объект? = Неопределено, OffsetFromStart: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-u", URL)
        Опции.Вставить("-a", Count)
        Опции.Вставить("-o", OffsetFromStart)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetPublicObject", Опции)
        
    ;

    @Глобально
    метод GetDownloadLinkForPublicObject(Token: Объект?, URL: Объект?, Path: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-u", URL)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetDownloadLinkForPublicObject", Опции)
        
    ;

    @Глобально
    метод SavePublicObjectToDisk(Token: Объект?, URL: Объект?, From: Объект? = Неопределено, Target: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", Token)
        Опции.Вставить("-u", URL)
        Опции.Вставить("-f", From)
        Опции.Вставить("-o", Target)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "SavePublicObjectToDisk", Опции)
        
    ;
    
;