#совместимость 9.0

#требуется ./internal/Executor.sbsl

@Глобально
метод Инициализировать(Конфигурация: Строка?|Executor.НастройкиИнтеграции): OPI_FTP
    
    пер Исполнитель = Executor.ИсполнительОПИ.Инициализировать(Конфигурация)
    возврат новый OPI_FTP(Исполнитель)
    
;

@Локально
структура OPI_FTP
    
    обз знч Исполнитель: Executor.ИсполнительОПИ
    
    знч КомандаCLI: Строка = "ftp"
    
    @Глобально
    метод CreateConnection(FTPSettings: Объект?, Proxy: Объект? = Неопределено, Tls: Объект? = Неопределено, Logging: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-s", FTPSettings)
        Опции.Вставить("-p", Proxy)
        Опции.Вставить("-t", Tls)
        Опции.Вставить("-l", Logging)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "CreateConnection", Опции)
        
    ;

    @Глобально
    метод GetConnectionConfiguration(FTPSettings: Объект?, Proxy: Объект? = Неопределено, Tls: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-s", FTPSettings)
        Опции.Вставить("-p", Proxy)
        Опции.Вставить("-t", Tls)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetConnectionConfiguration", Опции)
        
    ;

    @Глобально
    метод CloseConnection(Connection: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "CloseConnection", Опции)
        
    ;

    @Глобально
    метод GetWelcomeMessage(Connection: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetWelcomeMessage", Опции)
        
    ;

    @Глобально
    метод GetProtocolFeatureList(Connection: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetProtocolFeatureList", Опции)
        
    ;

    @Глобально
    метод Ping(Connection: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "Ping", Опции)
        
    ;

    @Глобально
    метод ExecuteCustomCommand(Connection: Объект?, CommandText: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-m", CommandText)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ExecuteCustomCommand", Опции)
        
    ;

    @Глобально
    метод ExecuteArbitraryCommand(Connection: Объект?, CommandText: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-m", CommandText)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ExecuteArbitraryCommand", Опции)
        
    ;

    @Глобально
    метод GetObjectSize(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetObjectSize", Опции)
        
    ;

    @Глобально
    метод UpdatePath(Connection: Объект?, Path: Объект?, NewPath: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-o", Path)
        Опции.Вставить("-n", NewPath)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "UpdatePath", Опции)
        
    ;

    @Глобально
    метод IsConnector(Value: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-v", Value)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "IsConnector", Опции)
        
    ;

    @Глобально
    метод GetConnectionSettings(Host: Объект?, Port: Объект? = Неопределено, Login: Объект? = Неопределено, Password: Объект? = Неопределено, Passive: Объект? = Неопределено, ReadTimeout: Объект? = Неопределено, WriteTimeout: Объект? = Неопределено, IPResolve: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-h", Host)
        Опции.Вставить("-p", Port)
        Опции.Вставить("-l", Login)
        Опции.Вставить("-s", Password)
        Опции.Вставить("-v", Passive)
        Опции.Вставить("-r", ReadTimeout)
        Опции.Вставить("-w", WriteTimeout)
        Опции.Вставить("-i", IPResolve)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetConnectionSettings", Опции)
        
    ;

    @Глобально
    метод GetProxySettings(Address: Объект?, Port: Объект?, View: Объект? = Неопределено, Login: Объект? = Неопределено, Password: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-a", Address)
        Опции.Вставить("-p", Port)
        Опции.Вставить("-t", View)
        Опции.Вставить("-l", Login)
        Опции.Вставить("-s", Password)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetProxySettings", Опции)
        
    ;

    @Глобально
    метод GetTlsSettings(DisableCertVerification: Объект?, CertFilepath: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-t", DisableCertVerification)
        Опции.Вставить("-c", CertFilepath)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetTlsSettings", Опции)
        
    ;

    @Глобально
    метод GetLoggingSettings(WriteToMemory: Объект? = Неопределено, MaxEvents: Объект? = Неопределено, FilePath: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-m", WriteToMemory)
        Опции.Вставить("-c", MaxEvents)
        Опции.Вставить("-p", FilePath)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetLoggingSettings", Опции)
        
    ;

    @Глобально
    метод GetLog(Connection: Объект?, AsString: Объект? = Неопределено, EventCount: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-s", AsString)
        Опции.Вставить("-n", EventCount)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetLog", Опции)
        
    ;

    @Глобально
    метод ListObjects(Connection: Объект?, Path: Объект? = Неопределено, Recursively: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-r", Recursively)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ListObjects", Опции)
        
    ;

    @Глобально
    метод CreateNewDirectory(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "CreateNewDirectory", Опции)
        
    ;

    @Глобально
    метод DeleteDirectory(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "DeleteDirectory", Опции)
        
    ;

    @Глобально
    метод ClearDirectory(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ClearDirectory", Опции)
        
    ;

    @Глобально
    метод GetCurrentDirectory(Connection: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetCurrentDirectory", Опции)
        
    ;

    @Глобально
    метод ChangeCurrentDirectory(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ChangeCurrentDirectory", Опции)
        
    ;

    @Глобально
    метод UploadFile(Connection: Объект?, File: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-f", File)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "UploadFile", Опции)
        
    ;

    @Глобально
    метод DeleteFile(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "DeleteFile", Опции)
        
    ;

    @Глобально
    метод SaveFile(Connection: Объект?, Path: Объект?, FileName: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        Опции.Вставить("-f", FileName)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "SaveFile", Опции)
        
    ;

    @Глобально
    метод GetFileData(Connection: Объект?, Path: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-c", Connection)
        Опции.Вставить("-p", Path)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "GetFileData", Опции)
        
    ;
    
;