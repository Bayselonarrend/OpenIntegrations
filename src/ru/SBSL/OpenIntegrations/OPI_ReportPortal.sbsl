#совместимость 8.0

#требуется ./internal/Executor.sbsl

@Глобально
метод Инициализировать(Конфигурация: Строка?|Executor.НастройкиИнтеграции): OPI_ReportPortal
    
    пер Исполнитель = Executor.ИсполнительОПИ.Инициализировать(Конфигурация)
    возврат новый OPI_ReportPortal(Исполнитель)
    
;

@Локально
структура OPI_ReportPortal
    
    обз знч Исполнитель: Executor.ИсполнительОПИ
    
    знч КомандаCLI: Строка = "rportal"
    
    @Глобально
    метод ПолучитьВременныйТокен(URL: Объект?, Логин: Объект?, Пароль: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-l", Логин)
        Опции.Вставить("-p", Пароль)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьВременныйТокен", Опции)
        
    ;

    @Глобально
    метод ПолучитьПостоянныйТокен(URL: Объект?, Токен: Объект?, IDПользователя: Объект?, ИмяКлюча: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-s", IDПользователя)
        Опции.Вставить("-n", ИмяКлюча)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьПостоянныйТокен", Опции)
        
    ;

    @Глобально
    метод УдалитьПостоянныйТокен(URL: Объект?, Токен: Объект?, IDПользователя: Объект?, IDКлюча: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-s", IDПользователя)
        Опции.Вставить("-i", IDКлюча)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "УдалитьПостоянныйТокен", Опции)
        
    ;

    @Глобально
    метод ПолучитьТокеныПользователя(URL: Объект?, Токен: Объект?, IDПользователя: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-s", IDПользователя)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьТокеныПользователя", Опции)
        
    ;

    @Глобально
    метод СоздатьПользователя(URL: Объект?, Токен: Объект?, СтруктураПользователя: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", СтруктураПользователя)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "СоздатьПользователя", Опции)
        
    ;

    @Глобально
    метод УдалитьПользователя(URL: Объект?, Токен: Объект?, IDПользователя: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-i", IDПользователя)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "УдалитьПользователя", Опции)
        
    ;

    @Глобально
    метод ИзменитьПользователя(URL: Объект?, Токен: Объект?, Логин: Объект?, СтруктураПользователя: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-l", Логин)
        Опции.Вставить("-p", СтруктураПользователя)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ИзменитьПользователя", Опции)
        
    ;

    @Глобально
    метод ПолучитьТекущегоПользователя(URL: Объект?, Токен: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьТекущегоПользователя", Опции)
        
    ;

    @Глобально
    метод ПолучитьПользователя(URL: Объект?, Токен: Объект?, Логин: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-l", Логин)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьПользователя", Опции)
        
    ;

    @Глобально
    метод ПолучитьПользователей(URL: Объект?, Токен: Объект?, Страница: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Страница)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьПользователей", Опции)
        
    ;

    @Глобально
    метод ПолучитьПроектыПользователя(URL: Объект?, Токен: Объект?, Логин: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-l", Логин)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьПроектыПользователя", Опции)
        
    ;

    @Глобально
    метод ПолучитьСтруктуруПользователя(Пустая: Объект? = Неопределено, КакСоответствие: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-e", Пустая)
        Опции.Вставить("-m", КакСоответствие)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьСтруктуруПользователя", Опции)
        
    ;

    @Глобально
    метод СоздатьПроект(URL: Объект?, Токен: Объект?, Имя: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-n", Имя)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "СоздатьПроект", Опции)
        
    ;

    @Глобально
    метод УдалитьПроект(URL: Объект?, Токен: Объект?, IDПроекта: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-i", IDПроекта)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "УдалитьПроект", Опции)
        
    ;

    @Глобально
    метод ПолучитьПроект(URL: Объект?, Токен: Объект?, Имя: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-n", Имя)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьПроект", Опции)
        
    ;

    @Глобально
    метод ПолучитьПроекты(URL: Объект?, Токен: Объект?, Страница: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Страница)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьПроекты", Опции)
        
    ;

    @Глобально
    метод ДобавитьПользователейВПроект(URL: Объект?, Токен: Объект?, ИмяПроекта: Объект?, СписокПользователей: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", ИмяПроекта)
        Опции.Вставить("-s", СписокПользователей)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ДобавитьПользователейВПроект", Опции)
        
    ;

    @Глобально
    метод ИсключитьПользователейИзПроекта(URL: Объект?, Токен: Объект?, ИмяПроекта: Объект?, МассивПользователей: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", ИмяПроекта)
        Опции.Вставить("-s", МассивПользователей)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ИсключитьПользователейИзПроекта", Опции)
        
    ;

    @Глобально
    метод СоздатьЗапуск(URL: Объект?, Токен: Объект?, Проект: Объект?, СтруктураЗапуска: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-r", СтруктураЗапуска)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "СоздатьЗапуск", Опции)
        
    ;

    @Глобально
    метод ПолучитьЗапуск(URL: Объект?, Токен: Объект?, Проект: Объект?, UUIDЗапуска: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-d", UUIDЗапуска)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьЗапуск", Опции)
        
    ;

    @Глобально
    метод ИзменитьЗапуск(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЗапуска: Объект?, СтруктураЗапуска: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЗапуска)
        Опции.Вставить("-r", СтруктураЗапуска)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ИзменитьЗапуск", Опции)
        
    ;

    @Глобально
    метод ЗавершитьЗапуск(URL: Объект?, Токен: Объект?, Проект: Объект?, UUIDЗапуска: Объект?, СтруктураЗавершения: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-d", UUIDЗапуска)
        Опции.Вставить("-r", СтруктураЗавершения)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ЗавершитьЗапуск", Опции)
        
    ;

    @Глобально
    метод УдалитьЗапуск(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЗапуска: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЗапуска)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "УдалитьЗапуск", Опции)
        
    ;

    @Глобально
    метод ПолучитьОтчетЗапуска(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЗапуска: Объект?, Формат: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЗапуска)
        Опции.Вставить("-f", Формат)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьОтчетЗапуска", Опции)
        
    ;

    @Глобально
    метод ПолучитьЭлементыЗапуска(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЗапуска: Объект?, Страница: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЗапуска)
        Опции.Вставить("-g", Страница)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьЭлементыЗапуска", Опции)
        
    ;

    @Глобально
    метод СоздатьЭлемент(URL: Объект?, Токен: Объект?, Проект: Объект?, СтруктураЭлемента: Объект?, Родитель: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-r", СтруктураЭлемента)
        Опции.Вставить("-n", Родитель)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "СоздатьЭлемент", Опции)
        
    ;

    @Глобально
    метод ПолучитьЭлемент(URL: Объект?, Токен: Объект?, Проект: Объект?, UUIDЭлемента: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-d", UUIDЭлемента)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьЭлемент", Опции)
        
    ;

    @Глобально
    метод ИзменитьЭлемент(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЭлемента: Объект?, СтруктураЭлемента: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЭлемента)
        Опции.Вставить("-r", СтруктураЭлемента)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ИзменитьЭлемент", Опции)
        
    ;

    @Глобально
    метод ЗавершитьЭлемент(URL: Объект?, Токен: Объект?, Проект: Объект?, UUIDЭлемента: Объект?, СтруктураЗавершения: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-d", UUIDЭлемента)
        Опции.Вставить("-r", СтруктураЗавершения)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ЗавершитьЭлемент", Опции)
        
    ;

    @Глобально
    метод УдалитьЭлемент(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЭлемента: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЭлемента)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "УдалитьЭлемент", Опции)
        
    ;

    @Глобально
    метод ПолучитьСтруктуруЗапуска(Пустая: Объект? = Неопределено, КакСоответствие: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-e", Пустая)
        Опции.Вставить("-m", КакСоответствие)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьСтруктуруЗапуска", Опции)
        
    ;

    @Глобально
    метод ПолучитьСтруктуруЭлемента(Пустая: Объект? = Неопределено, КакСоответствие: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-e", Пустая)
        Опции.Вставить("-m", КакСоответствие)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьСтруктуруЭлемента", Опции)
        
    ;

    @Глобально
    метод ПолучитьСтруктуруЗавершенияЭлемента(Пустая: Объект? = Неопределено, КакСоответствие: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-e", Пустая)
        Опции.Вставить("-m", КакСоответствие)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьСтруктуруЗавершенияЭлемента", Опции)
        
    ;

    @Глобально
    метод ПолучитьСтруктуруЗавершенияЗапуска(Время: Объект?, Статус: Объект? = Неопределено, Описание: Объект? = Неопределено, Атрибуты: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-e", Время)
        Опции.Вставить("-s", Статус)
        Опции.Вставить("-d", Описание)
        Опции.Вставить("-a", Атрибуты)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьСтруктуруЗавершенияЗапуска", Опции)
        
    ;

    @Глобально
    метод ЗаписатьЛог(URL: Объект?, Токен: Объект?, Проект: Объект?, СтруктураЛога: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-r", СтруктураЛога)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ЗаписатьЛог", Опции)
        
    ;

    @Глобально
    метод ПолучитьЛог(URL: Объект?, Токен: Объект?, Проект: Объект?, UUIDЛога: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-d", UUIDЛога)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьЛог", Опции)
        
    ;

    @Глобально
    метод ПолучитьЛогиЭлемента(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЭлемента: Объект?, Страница: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЭлемента)
        Опции.Вставить("-g", Страница)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьЛогиЭлемента", Опции)
        
    ;

    @Глобально
    метод УдалитьЛог(URL: Объект?, Токен: Объект?, Проект: Объект?, IDЛога: Объект?): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-u", URL)
        Опции.Вставить("-t", Токен)
        Опции.Вставить("-p", Проект)
        Опции.Вставить("-i", IDЛога)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "УдалитьЛог", Опции)
        
    ;

    @Глобально
    метод ПолучитьСтруктуруЛога(IDЗапуска: Объект?, IDЭлемента: Объект?, Время: Объект?, Текст: Объект? = Неопределено, Уровень: Объект? = Неопределено): Объект?
        
        пер Опции = новый Соответствие<Строка, Объект?>() 
        Опции.Вставить("-l", IDЗапуска)
        Опции.Вставить("-i", IDЭлемента)
        Опции.Вставить("-t", Время)
        Опции.Вставить("-x", Текст)
        Опции.Вставить("-v", Уровень)
        
        возврат Исполнитель.Выполнить(КомандаCLI, "ПолучитьСтруктуруЛога", Опции)
        
    ;
    
;