pipeline {
    agent {
        label 'windows'
    }
    stages {
        stage('Release') {
            steps {
                bat 'oscript ./ci/os/internal/Classes/ReleaseFactory.os'
            }
        }

        stage('Archive') {
            steps {
                archiveArtifacts artifacts: '1.32.0/*'
            }
        }
    }
}