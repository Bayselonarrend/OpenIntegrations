pipeline {
    agent any
    stages {

        stage('Release') {
            steps {
                    bat encoding: 'UTF-8', script:'oscript ./ci/os/releasemaker.os'
            }
        }
    }
}