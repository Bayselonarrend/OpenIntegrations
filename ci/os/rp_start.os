#Использовать oint

Если АргументыКоманднойСтроки.Количество() > 0 Тогда
	Платформа = СокрЛП(АргументыКоманднойСтроки[0]);
Иначе
	Платформа = "";
КонецЕсли;

Попытка
	ПДТ = OPI_ПолучениеДанныхТестов;
Исключение
	ПДТ = OPI_TestDataRetrieval;
КонецПопытки;

ПДТ.СоздатьЗапускReportPortal(Платформа);