    URL = "https://bin.openintegrations.dev";
    URL = URL + "/range/100";

    // Get bytes 0-9
    StartPosition = 0;
    Bytes = 10;

    Result = OPI_HTTPRequests.NewRequest()
        .Initialize(URL)
        .GetPart(StartPosition, Bytes) // <---
        .ReturnResponse();