---
sidebar_position: 2

description: Получить сообщения и другие функции для работы с Ntfy в Открытом пакете интеграций - бесплатной open-source библиотеке интеграций для 1С:Предприятие 8, OneScript и CLI
keywords: [1C, 1С, 1С:Предприятие, 1С:Предприятие 8.3, API, Интеграция, Сервисы, Обмен, OneScript, CLI, Ntfy]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import Admonition from '@theme/Admonition';

# Получить сообщения
 Получает сообщения из топика в формате JSON



<Tabs>
  <TabItem value="params" label="Параметры" default>

`Функция ПолучитьСообщения(Знач URL, Знач Топик, Знач Токен = "", Знач Фильтр = "") Экспорт`

  | Параметр | CLI опция | Тип | Обяз. | Назначение |
  |-|-|-|-|-|
  | URL | --url | Строка | &#x2714; | URL сервера Ntfy. `ntfy.sh`, если не заполнено |
  | Топик | --topic | Строка | &#x2714; | Имя топика |
  | Токен | --token | Строка | &#x2716; | Токен доступа к топику, если требуется |
  | Фильтр | --filter | Структура Из КлючИЗначение | &#x2716; | Параметры отбора. См. [`ПолучитьСтруктуруПараметровФильтра`](/docs/Ntfy/Obtaining/Get-filter-parameters-structure) |


<div className="return-value-note">
  <div className="return-value-note__title">Возвращаемое значение</div>
  <div className="return-value-note__value">
  Массив - список сообщений топика
  </div>
</div>
  
  </TabItem>
  <TabItem value="extended" label={<span>Расширенный вызов{' '}<a href="/docs/Start/Advanced-call" target="_blank" rel="noreferrer" title="Справка о расширенном вызове" onClick={(e) => e.stopPropagation()}>?</a></span>}>

  | Параметр | Описание |
  |---|---|
  | proxy | `ИнтернетПрокси` или структура полей `Протокол`, `Сервер`, `Порт`, `Пользователь`, `Пароль`, `ИспользоватьАутентификациюОС` |
  | timeout | Таймаут выполнения запроса |
  | adv_response | Оформляет ответ в виде полной структуры HTTP-ответа с полями `code`, `body` и `headers` |
  | retries | Число попыток отправки HTTP-запроса при коде `5**` или внутренних ошибках клиента |
  | dontwait | Создает фоновое задание и возвращает его данные (только для 1С и OneScript) |


  </TabItem>
</Tabs>

:::tip
Документация ntfy API: [docs.ntfy.sh/subscribe/api](https://docs.ntfy.sh/subscribe/api)
:::
<br/>


```bsl title="Пример использования для 1С:Предприятие/OneScript"
    URL   = "https://ntfy.sh";
    Топик = "OpenIntegrationsTest";
    Токен = "...";

    Фильтр       = OPI_Ntfy.ПолучитьСтруктуруПараметровФильтра(Истина);
    Фильтр.limit = "5";

    Результат = OPI_Ntfy.ПолучитьСообщения(URL, Топик, Токен, Фильтр);

    OPI_ПолучениеДанныхТестов.Обработать(Результат, "Ntfy", "ПолучитьСообщения");
```
    




