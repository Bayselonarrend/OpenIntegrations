---
sidebar_position: 3
description: Получить метод и другие функции для работы с GRPC в Открытом пакете интеграций - бесплатной open-source библиотеке интеграций для 1С:Предприятие 8, OneScript и CLI
keywords: [1C, 1С, 1С:Предприятие, 1С:Предприятие 8.3, API, Интеграция, Сервисы, Обмен, OneScript, CLI, GRPC]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Получить метод
 Получает информацию о методе



`Функция ПолучитьМетод(Знач Соединение, Знач Сервис, Знач Метод, Знач Tls = Неопределено) Экспорт`

  | Параметр | CLI опция | Тип | Обяз. | Назначение |
  |-|-|-|-|-|
  | Соединение | --conn | Произвольный | &#x2714; | Существующее соединение или параметры соединения |
  | Сервис | --service | Строка | &#x2714; | Имя сервиса |
  | Метод | --method | Строка | &#x2714; | Имя метода |
  | Tls | --tls | Структура Из КлючИЗначение | &#x2716; | Настройки TLS, если необходимо. См. [`ПолучитьНастройкиTls`](/docs/GRPC/Common-methods/Get-tls-settings) |

  
  Возвращаемое значение:   Соответствие Из КлючИЗначение - Результат обработки



```bsl title="Пример использования для 1С:Предприятие/OneScript"
    Адрес = "https://grpcb.in:9001";
    Схема = "https://hut.openintegrations.dev/test_data/grpcbin.proto"; // Строка, путь к файлу или URL

    Опции = Новый Структура;
    Опции.Вставить("addr" , Адрес);
    Опции.Вставить("proto", Схема);

    Параметры = OPI_ПолучениеДанныхТестов.ВыполнитьТестCLI("grpc", "ПолучитьПараметрыСоединения", Опции);
    Опции     = Новый Структура;
    Опции.Вставить("trust", Истина);

    Tls = OPI_ПолучениеДанныхТестов.ВыполнитьТестCLI("grpc", "ПолучитьНастройкиTls", Опции);

    Соединение = OPI_GRPC.ОткрытьСоединение(Параметры, Tls);
    Сервис     = "grpcbin.GRPCBin";
    Метод      = "Index";

    Опции = Новый Структура;
    Опции.Вставить("conn"   , Соединение);
    Опции.Вставить("service", Сервис);
    Опции.Вставить("method" , Метод);

    Результат = OPI_ПолучениеДанныхТестов.ВыполнитьТестCLI("grpc", "ПолучитьМетод", Опции);
```
    

 <Tabs>
  
    <TabItem value="bash" label="Bash" default>
        ```bash
            # JSON данные также могут быть переданы как путь к файлу .json
            
            oint grpc ПолучитьМетод \
              --conn "{'address':'https://grpcb.in:9001','proto':{'main.proto':'https://hut.openintegrations.dev/test_data/grpcbin.proto'}}" \
              --service "grpcbin.GRPCBin" \
              --method "Index"
        ```
    </TabItem>
  
    <TabItem value="bat" label="CMD/Bat" default>
        ```batch
            :: JSON данные также могут быть переданы как путь к файлу .json
            
            oint grpc ПолучитьМетод ^
              --conn "{'address':'https://grpcb.in:9001','proto':{'main.proto':'https://hut.openintegrations.dev/test_data/grpcbin.proto'}}" ^
              --service "grpcbin.GRPCBin" ^
              --method "Index"
        ```
    </TabItem>
</Tabs>


```json title="Результат"
{
 "data": {
  "is_client_streaming": "***",
  "is_server_streaming": false,
  "request_schema": {
   "fields": {},
   "type": "object"
  },
  "request_type": "grpcbin.EmptyMessage",
  "response_schema": {
   "fields": {
    "description": {
     "number": 1,
     "required": true,
     "type": "string"
    },
    "endpoints": {
     "number": 2,
     "required": true,
     "type": "message"
    }
   },
   "type": "object"
  },
  "response_type": "grpcbin.IndexReply"
 },
 "result": true
}
```
