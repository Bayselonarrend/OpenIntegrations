---
sidebar_position: 14
description: Получить структуру элемента и другие функции для работы с ReportPortal в Открытом пакете интеграций - бесплатной open-source библиотеке интеграций для 1С:Предприятие 8, OneScript и CLI
keywords: [1C, 1С, 1С:Предприятие, 1С:Предприятие 8.3, API, Интеграция, Сервисы, Обмен, OneScript, CLI, ReportPortal]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Получить структуру элемента
 Получает структуру параметров тестового элемента



`Функция ПолучитьСтруктуруЭлемента(Знач Пустая = Ложь, Знач КакСоответствие = Ложь) Экспорт`

  | Параметр | CLI опция | Тип | Обяз. | Назначение |
  |-|-|-|-|-|
  | Пустая | --empty | Булево | &#x2716; | Истина > структура с пустыми значениями, Ложь > в значениях будут описания полей |
  | КакСоответствие | --map | Булево | &#x2716; | Истина > возвращает поля фильтра как соответствие |

  
  Возвращаемое значение:   Структура Из КлючИЗначение - Структура полей



```bsl title="Пример использования для 1С:Предприятие/OneScript"
    Результат = OPI_ReportPortal.ПолучитьСтруктуруЭлемента();
```
    

 <Tabs>
  
    <TabItem value="bash" label="Bash" default>
        ```bash
            oint rportal ПолучитьСтруктуруЭлемента \
              --empty true
        ```
    </TabItem>
  
    <TabItem value="bat" label="CMD/Bat" default>
        ```batch
            oint rportal ПолучитьСтруктуруЭлемента ^
              --empty true
        ```
    </TabItem>
</Tabs>


```json title="Результат"
{
 "name": "<название элемента>",
 "startTime": "<время начала>",
 "type": "<тип элемента: suite, story, test, scenario, step, before_class, before_groups, before_method, before_suite, before_test, after_class, after_groups, after_method, after_suite, after_test>",
 "launchUuid": "<UUID запуска>",
 "description": "<описание элемента>",
 "attributes": "<атрибуты элемента в виде ключ1:значение1, ключ2:значение2>",
 "uuid": "<UUID элемента. Будет сгенерирован автоматически, если не заполнено>",
 "codeRef": "<физическое расположение тестов>",
 "parameters": "<параметры для параметризованных тестов>",
 "retry": "<является перезапуском>"
}
```
