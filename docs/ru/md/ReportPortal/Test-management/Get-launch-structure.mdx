---
sidebar_position: 13
description: Получить структуру запуска и другие функции для работы с ReportPortal в Открытом пакете интеграций - бесплатной open-source библиотеке интеграций для 1С:Предприятие 8, OneScript и CLI
keywords: [1C, 1С, 1С:Предприятие, 1С:Предприятие 8.3, API, Интеграция, Сервисы, Обмен, OneScript, CLI, ReportPortal]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Получить структуру запуска
 Получает структуру параметров запуска



`Функция ПолучитьСтруктуруЗапуска(Знач Пустая = Ложь, Знач КакСоответствие = Ложь) Экспорт`

  | Параметр | CLI опция | Тип | Обяз. | Назначение |
  |-|-|-|-|-|
  | Пустая | --empty | Булево | &#x2716; | Истина > структура с пустыми значениями, Ложь > в значениях будут описания полей |
  | КакСоответствие | --map | Булево | &#x2716; | Истина > возвращает поля фильтра как соответствие |

  
  Возвращаемое значение:   Структура Из КлючИЗначение - Структура полей



```bsl title="Пример использования для 1С:Предприятие/OneScript"
    Результат = OPI_ReportPortal.ПолучитьСтруктуруЗапуска();
```
    

 <Tabs>
  
    <TabItem value="bash" label="Bash" default>
        ```bash
            oint rportal ПолучитьСтруктуруЗапуска \
              --empty true
        ```
    </TabItem>
  
    <TabItem value="bat" label="CMD/Bat" default>
        ```batch
            oint rportal ПолучитьСтруктуруЗапуска ^
              --empty true
        ```
    </TabItem>
</Tabs>


```json title="Результат"
{
 "name": "<название запуска>",
 "startTime": "<время начала запуска>",
 "description": "<описание запуска>",
 "uuid": "<UUID запуска. Будет сгенерирован автоматически, если не заполнено>",
 "attributes": "<атрибуты запуска в виде ключ1:значение1, ключ2:значение2>",
 "mode": "<режим: default или debug>",
 "rerun": "<признак перезапуска>",
 "rerunOf": "<UUID уже существующего запуска, если rerun = Истина>"
}
```
