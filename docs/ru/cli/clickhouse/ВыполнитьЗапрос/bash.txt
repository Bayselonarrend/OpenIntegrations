# JSON данные также могут быть переданы как путь к файлу .json

oint clickhouse ВыполнитьЗапрос \
  --conn "{'address':'http://127.0.0.1:9101','auth_type':'basic','password':'***','transport':'grpc','user':'bayselonarrend'}" \
  --req "{'query':'CREATE TABLE IF NOT EXISTS events_grpc (\n    id UInt64,\n    timestamp DateTime,\n    user_id UInt32,\n    event_type String,\n    payload String\n) ENGINE    = MergeTree()\nORDER BY (timestamp, id)','format':'JSON'}" \
  --tls "{}"