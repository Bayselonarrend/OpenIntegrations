:: JSON данные также могут быть переданы как путь к файлу .json

oint clickhouse ВыполнитьЗапрос ^
  --conn "{'address':'http://localhost:8123','transport':'http','auth_type':'basic','user':'bayselonarrend','password':'***'}" ^
  --req "{'query':'CREATE TABLE IF NOT EXISTS events (\n    id UInt64,\n    timestamp DateTime,\n    user_id UInt32,\n    event_type String,\n    payload String\n) ENGINE    = MergeTree()\nORDER BY (timestamp, id)','format':'JSON'}"