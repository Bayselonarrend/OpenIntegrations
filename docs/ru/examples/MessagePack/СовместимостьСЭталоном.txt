    БазовыйURL = MessagePackБазовыйURLЭталона();

    Результат = OPI_ЗапросыHTTP.НовыйЗапрос()
        .Инициализировать(БазовыйURL + "/health")
        .ОбработатьЗапрос("GET")
        .ВернутьОтветКакJSONКоллекцию();