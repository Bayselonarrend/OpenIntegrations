    URL = "https://bin.openintegrations.dev";
    URL = URL + "/post";

    СлучайныйМассив = Новый Массив;
    СлучайныйМассив.Добавить("A");
    СлучайныйМассив.Добавить("B");
    СлучайныйМассив.Добавить("C");

    Результат = OPI_ЗапросыHTTP.НовыйЗапрос()
        .Инициализировать(URL)
        .ИнициализироватьJsonТело() // <---
        .ВставитьПолеТела("Поле1", 10)
        .ВставитьПолеТела("Поле2", "Текст")
        .ВставитьПолеТела("Поле3", СлучайныйМассив)
        .ОбработатьЗапрос("POST")
        .ВернутьОтветКакJSONКоллекцию();