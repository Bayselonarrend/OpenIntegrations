    ФайлЛога             = ПолучитьИмяВременногоФайла("txt");
    НастройкиЛогирования = OPI_SQLite.ПолучитьНастройкиЛогирования(Истина, 100, ФайлЛога);
    ИВФ                  = ПолучитьИмяВременногоФайла("sqlite");

    Соединение = OPI_SQLite.ОткрытьСоединение(ИВФ, НастройкиЛогирования);

    Если Не OPI_SQLite.ЭтоКоннектор(Соединение) Тогда
        ВызватьИсключение OPI_Инструменты.JSONСтрокой(Соединение);
    КонецЕсли;

    Результат = OPI_SQLite.ВыполнитьЗапросSQL("SELECT 1 AS n", , , Соединение);

    Результат = OPI_SQLite.ПолучитьЛог(Соединение);