    // Сервер
    Порт          = 5555;
    ОбъектСервера = OPI_ZeroMQ.ОткрытьПортPush(Порт);

    Если Не OPI_ZeroMQ.ЭтоОбъектКоннектора(ОбъектСервера) Тогда
        ВызватьИсключение OPI_Инструменты.JSONСтрокой(ОбъектСервера);
    КонецЕсли;

    // Клиент
    Адрес     = "tcp://127.0.0.1:5555";
    Результат = OPI_ZeroMQ.СоздатьСоединениеPull(Адрес);

    Если Не OPI_ZeroMQ.ЭтоОбъектКоннектора(Результат) Тогда
        ВызватьИсключение OPI_Инструменты.JSONСтрокой(Результат);
    КонецЕсли;

    Сообщение = СтрШаблон("ZMQ_C_PULL_%1", Формат(ТекущаяДата(), "ДФ=yyyyMMddhhmmss"));
    Данные = ПолучитьДвоичныеДанныеИзСтроки(Сообщение);

    Отправка = OPI_ZeroMQ.ОтправитьДанные(ОбъектСервера, Данные, 3000);
    Проверка = OPI_ZeroMQ.ПолучитьДанные(Результат, 3000);